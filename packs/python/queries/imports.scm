; Import statements. Field extraction (module_name, name, alias, relative
; prefix) happens in src/language/imports.ts against the captured node.
(import_statement) @import
(import_from_statement) @from_import
